function Hero(name, level)
{
name = name;
level = level;
}

console.log(Hero.name);
console.log(Hero.level);

Output: 
Hero
undefined

Explanation:
Both can not be the same one, and hence it throws garbage output.